# 1 K8's version - apiVersion - apps/v1
apiVersion: apps/v1

# 2 Type of K8's Component - Deployment
kind: Deployment

# 3 Metadata of K8's
metadata:
  name: deployment1
  labels:
    app: deployment1-nginx

# 4 Specification of your app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pod1-nginx
  template:
    # 3 Metadata of K8's
    metadata:
      name: pod1
      labels:
        app: pod1-nginx

    # 4 Specification of your app
    spec:
      containers:
        # Here is my containers list
        - name: nginx-container
          image: nginx:1.25
          ports:
            - containerPort: 80
  